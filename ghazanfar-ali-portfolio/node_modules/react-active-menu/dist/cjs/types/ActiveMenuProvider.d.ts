import type { PropsWithChildren } from "react";
import type { ActiveMenuValues } from "./types";
export interface ActiveMenuProviderProps {
    menu: ActiveMenuValues;
}
export declare const ActiveMenuProvider: ({ children, menu }: PropsWithChildren<ActiveMenuProviderProps>) => JSX.Element;
